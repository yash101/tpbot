# Basic C/C++ program which implements the robot API since we can't do hwdev rn
# This sim should also help more quickly get robot's APIs finalized and can be ported
# into the robot after respin + bringup
